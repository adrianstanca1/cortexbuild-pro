# ============================================================
# CortexBuild Pro - Docker Compose Configuration
# ============================================================
# 
# REQUIRED ENVIRONMENT VARIABLES (must be set before running):
#   - NEXTAUTH_SECRET: Generate with `openssl rand -base64 32`
#   - ABACUSAI_API_KEY: Your AbacusAI API key (for AI features)
#
# OPTIONAL ENVIRONMENT VARIABLES (have defaults):
#   - POSTGRES_USER: Database username (default: cortexbuild)
#   - POSTGRES_PASSWORD: Database password (default: CortexSecure2026)
#   - POSTGRES_DB: Database name (default: cortexbuild)
#   - NEXTAUTH_URL: Application URL (default: https://cortexbuildpro.com)
#   - ENCRYPTION_KEY: Data encryption key
#
# Usage:
#   # Set required variables and run
#   export NEXTAUTH_SECRET=$(openssl rand -base64 32)
#   export ABACUSAI_API_KEY=your-api-key
#   docker compose up -d
#
# Or use an .env file in the deployment directory:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose --env-file .env up -d
# ============================================================

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: cortexbuild-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cortexbuild}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-CortexSecure2026}
      POSTGRES_DB: ${POSTGRES_DB:-cortexbuild}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backups:/backups
    ports:
      - "5433:5432"
    networks:
      - cortexbuild-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cortexbuild} -d ${POSTGRES_DB:-cortexbuild}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Next.js Application
  app:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    container_name: cortexbuild-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://${POSTGRES_USER:-cortexbuild}:${POSTGRES_PASSWORD:-CortexSecure2026}@db:5432/${POSTGRES_DB:-cortexbuild}?schema=public
      - NEXTAUTH_URL=${NEXTAUTH_URL:-https://cortexbuildpro.com}
      # REQUIRED: Generate with `openssl rand -base64 32`
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:?NEXTAUTH_SECRET is required - generate with: openssl rand -base64 32}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-cortexbuild_encryption_key_2026}
      # REQUIRED: Your AbacusAI API key for AI features
      - ABACUSAI_API_KEY=${ABACUSAI_API_KEY:-}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cortexbuild-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

networks:
  cortexbuild-network:
    driver: bridge

volumes:
  postgres_data:
