name: Fix Auth Redirect

on:
  push:
    branches:
      - cortexbuildpro

jobs:
  fix-auth-redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Fix Auth Redirect Script
        run: scripts/fix-auth-redirect.sh
